{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "startCommand": "sh -c 'echo \"PORT variable: $PORT\" && echo \"Starting server...\" && python server.py --port $PORT --host 0.0.0.0'"
}